import 'package:flutter/material.dart';

void main() {
  runApp(const NotesApp());
}

class NotesApp extends StatelessWidget {
  const NotesApp() : super();
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
